<form class="ticket-reporter" onSubmit="reportTicket" report-submit>
  <view class="button-wrapper">
    <button a:if="{{!hideButton}}" class="button" form-type="submit" onTap="handleTap" />
    <slot></slot>
  </view>
</form>
